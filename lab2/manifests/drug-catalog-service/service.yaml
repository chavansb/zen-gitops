apiVersion: v1
kind: Service
metadata:
  name: drug-catalog-service
  namespace: dev
  labels:
    app: drug-catalog-service
    env: dev
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: drug-catalog-service

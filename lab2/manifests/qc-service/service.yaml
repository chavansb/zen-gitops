apiVersion: v1
kind: Service
metadata:
  name: qc-service
  namespace: dev
  labels:
    app: qc-service
    env: dev
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
      protocol: TCP
      name: http
  selector:
    app: qc-service

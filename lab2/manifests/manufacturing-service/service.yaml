apiVersion: v1
kind: Service
metadata:
  name: manufacturing-service
  namespace: dev
  labels:
    app: manufacturing-service
    env: dev
spec:
  type: ClusterIP
  ports:
    - port: 8085
      targetPort: 8085
      protocol: TCP
      name: http
  selector:
    app: manufacturing-service

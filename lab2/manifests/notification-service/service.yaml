apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: dev
  labels:
    app: notification-service
    env: dev
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: notification-service

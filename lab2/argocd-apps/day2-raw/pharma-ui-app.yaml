apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pharma-ui-dev-raw
  namespace: argocd
  labels:
    env: dev
    app: pharma-ui
    phase: day2-raw
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: pharma

  source:
    repoURL: https://github.com/DPP-2026/zen-gitops.git
    targetRevision: HEAD
    path: lab2/manifests/pharma-ui

  destination:
    server: https://kubernetes.default.svc
    namespace: dev

  syncPolicy:
    automated:
      prune: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10
